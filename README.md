# networkTCP (combined project)

This workspace contains:
- `networkserver/` and `networkClient/` — original Java TCP GUI translator demo (Swing)
- `web-app/` — modern Translator web app (React + Node + MongoDB)

See `web-app/README.md` for running the web application locally and deploying.
